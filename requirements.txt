# Core — Claude API
anthropic>=0.40.0

# Gmail API backend
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# Word document generation (generate_report.py)
python-docx>=1.1.0

# IMAP backend uses Python's built-in imaplib — no extra packages required
